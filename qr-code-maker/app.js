(() => {
  const content=document.getElementById('content'),size=document.getElementById('size'),level=document.getElementById('level'),generateBtn=document.getElementById('generateBtn'),error=document.getElementById('error'),result=document.getElementById('result'),canvas=document.getElementById('qrCanvas'),download=document.getElementById('download'),copyBtn=document.getElementById('copyBtn'),copyStatus=document.getElementById('copyStatus');
  async function generate(){const text=content.value.trim();error.textContent='';copyStatus.textContent='';if(!text){error.textContent='Enter a link or some text first.';result.hidden=true;return;}if(!window.QRCode||typeof window.QRCode.toCanvas!=='function'){error.textContent='The QR library could not be loaded. Check your connection and try again.';return;}generateBtn.disabled=true;try{await window.QRCode.toCanvas(canvas,text,{width:Number(size.value),margin:2,errorCorrectionLevel:level.value});download.href=canvas.toDataURL('image/png');download.download='qr-code.png';result.hidden=false;}catch{error.textContent='The QR code could not be created. Try shorter text.';result.hidden=true;}finally{generateBtn.disabled=false;}}
  async function copy(){const text=content.value.trim();if(!text)return;try{await navigator.clipboard.writeText(text);copyStatus.textContent='Text copied.';}catch{copyStatus.textContent='Copy is not available in this browser.';}}
  generateBtn.addEventListener('click',generate);copyBtn.addEventListener('click',copy);content.addEventListener('keydown',e=>{if((e.ctrlKey||e.metaKey)&&e.key==='Enter')generate();});
})();